# API Configuration
# Leave empty for relative URLs (same origin as the frontend)
# Set to full URL for different origin (e.g., http://localhost:8000)
VITE_API_URL=

# WebSocket Configuration  
# Leave empty for relative URLs (same origin as the frontend)
# Set to full URL for different origin (e.g., ws://localhost:8000/ws)
VITE_WS_URL= 